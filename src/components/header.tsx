import { TonConnectButton } from '@tonconnect/ui-react'

function Header() {
    return (
        <header className="header">
            <TonConnectButton />
        </header>
    )
}

export default Header